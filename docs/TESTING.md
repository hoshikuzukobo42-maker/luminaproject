# 公開コピーのオフライン試験

確認日：2026年9月10日。以下はこの公開コピーで実際に再実行した結果です。本体で過去に行った試験、実モデルの認識精度、実機速度の測定とは別です。

| 対象 | 成功 | 補助ケース成功 | 省略 | 失敗 | 検証時の時間 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 最小4スイート | 163 | 107 | 0 | 0 | 0.33秒 |
| 拡張24スイート | 899 | 256 | 2 | 0 | 4.50秒 |

両方とも終了コード0。拡張試験には既存の非推奨警告61件があります。秒数は試験処理の時間で、Qwen推論の速度ではありません。Python 3.12.14・macOSで確認しました。他OSの動作確認はしていません。

## 最小試験

Python 3.12を用意し、このリポジトリのルートで実行します。

```sh
python3.12 -m venv .venv
.venv/bin/python -m pip install -r requirements-minimal-test.txt
.venv/bin/python scripts/run_public_tests.py
```

この4スイートは、共通入出力の検証、深度・地図・経路計画、単一対象の指示解釈、ラベル正規化を扱います。標準ライブラリとpytest自身の依存以外を読み込めない条件でも、163件と補助107件の成功を別途確認しました。モデル、画像素材、Godot、GPUは不要です。

## 拡張試験

```sh
.venv/bin/python -m pip install -r requirements-test.txt
.venv/bin/python scripts/run_public_tests.py --extended
```

24スイートには視覚・移動の接続、到着判定、API、自律行動の受付・結果回収等が含まれます。モデル応答や通信は試験用の値・実装に置き換えています。

`requirements-test.txt` は検証時の直接依存バージョンです。推移的依存を全て固定したロックファイルではなく、完全なアプリ実行環境の依存一覧でもありません。今回の拡張試験は既存のPython環境を用いて実行しており、全く新しい環境での依存インストールからの検証は別途必要です。

省略される2件は次のとおりです。成功件数に含めていません。

- 過去の実機Case46の記録を読む確認：生記録を公開しないため。
- llama.cppのスキーマ変換器の互換確認：外部ツール本体を同梱しないため。

本体側の過去結果は901件成功・省略なしですが、公開コピーでは899件成功・2件省略です。

## 誤接続・誤起動の予防

試験入口は、Lumina関連の既存環境変数を試験へ持ち込まないようにし、設定・一時DBを新規一時ディレクトリへ分離します。pytestプラグインの自動読み込み、Pythonのバイトコード出力、pytestキャッシュを無効にします。

また、監査フックでソケット接続や実プロセス起動等を検出・拒否します。今回の最小・拡張試験のいずれも禁止操作の検出は0件でした。これは今回のコードの誤接続を見つける補助であり、悪意あるコードを隔離する完全なサンドボックスではありません。

実機のモデル・音声サーバー・Godotは起動していません。音声起動スクリプトが同梱されていても、試験では設定契約をテキストとして読むだけです。

ログが必要な場合は、リポジトリの外に新しい出力名を指定できます。

```sh
.venv/bin/python scripts/run_public_tests.py --extended --log ../public-test.log --xml ../public-test.xml
```

既存ファイルの上書きと、公開リポジトリ内部へのログ出力は拒否します。生ログには端末のパスが入り得るため、そのまま公開対象に追加しないでください。

## 公開内容の点検

```sh
.venv/bin/python scripts/check_public_snapshot.py
```

抽出ソースのハッシュ、Python構文、想定外のファイル種類、典型的な機密パターンを読み取り専用で確認します。値は表示せず検出箇所だけを報告します。秘密情報や権利関係の網羅的な保証ではないため、更新時にも人による公開範囲の確認が必要です。
